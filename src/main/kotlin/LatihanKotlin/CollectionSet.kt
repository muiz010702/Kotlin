package LatihanKotlin

/*
    Kotlin set adalah kumpulan elemen atau item yang tidak berurutan dan bernilai unik
    Kumpulan Kotlin yang dapat berubah atau tidak dapat diubah tidak mengizinkan adanya elemen duplikat
    Kumpulan Kotlin dapat berupa mutable (mutableSetOf) atau read-only (setOf).
    Berguna ketika Anda tidak menginginkan data yang identik atau duplikat dalam suatu koleksi.
    Untuk pembuatan set, gunakan fungsi perpustakaan standar setOf() untuk set read-only.
    Fungsi setOf secara otomatis akan membuang nomor yang sama.
    Urutan himpunan tidak penting, jika Anda membandingkan dua himpunan dengan nilai yang sama, maka keduanya akan menjadi penting
    dianggap setara.
*/

fun main() {
//    var A = setOf(7,5,4,3,2,6,6,8)
//    var B = setOf(5,4,3,8,2)
//
//    var G = A.intersect(B) --> intersect digunakan untuk menampilkan bilangan yang sama
//    var H = B.union(A)--> union digunakan untuk menggabungkan kedua elemen
//
//    println(G)
//    println(H)
//    var C = setOf(1,2,3,4,5,4,3,6)
//    var D = setOf(1,2,3,4,5,6)
//    println(C == D)
//    var E = setOf('A','B','C','D')
//    var F = setOf('c','d','a','b')
//    println(E == F)--> Hasilnya false karena pada kedua elemen menampilkan bilangan yang berbeda
//    var Hewan = mutableSetOf(200,"Burung",12.6,'H')
//    println(200 in Hewan)

}